{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run p1.c (WSL)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/prog1.out",
      "args": ["input.txt", "output.txt"],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
